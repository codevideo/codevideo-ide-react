# node modules
node_modules

# dist
dist

# golden render outputs (mp4s are regenerable; archive the golden out-of-band)
scripts/golden/out/

# macOS
.DS_Store

# rollup bundle-analyzer output (regenerable)
stats.html
